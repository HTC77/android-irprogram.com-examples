<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".SpeakingActivity"
        android:orientation="vertical">
    <LinearLayout
            android:id="@+id/llMessages"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
                android:orientation="vertical">
        <ListView
                android:id="@+id/lvMessages"
                android:background="@null"
                android:divider="@null"
                android:layout_width="match_parent"
                android:layout_height="427dp"
                android:transcriptMode="alwaysScroll"
                android:stackFromBottom="true">
        </ListView>
        <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:weightSum="3"
                android:background="@color/white"
                 >
            <EditText
                    android:id="@+id/etMessage"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="2"
                    android:textColor="@color/message_input"
                    android:background="@color/message_input_bg"
                    android:textSize="22sp"
                    android:paddingHorizontal="6dp"
                    android:gravity="bottom"
            />
            <Button
                    android:id="@+id/btnSend"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:background="@color/btn_send"
                    android:textColor="@color/white"
                    android:layout_weight="1"
                    android:textSize="22sp"
                    android:text="@string/btn_send" android:onClick="onBtnSendClick"/>
        </LinearLayout>
    </LinearLayout>

</RelativeLayout>